import React, {memo} from "react";

const NormalFeed = memo(() => {
  return (
    <div className="text-slate-600 font-medium">
      This is fast, normal component
    </div>
  );
});

export default NormalFeed;
